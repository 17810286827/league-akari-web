# League Akari Web

车队（LoL 开黑小队）战绩查询与统计的 Web 端：搜索召唤师、查看对局详情、聚合车队周报与榜单中心。视觉语言为「海克斯魔典」（见 docs/adr/0002）。

## Language

### 接口契约

**业务失败**:
后端 HTTP 200 但统一信封 `code ≠ 0` 的失败（错误码登记在后端 ErrorCode：1xxx 参数 / 2xxx 对局 / 4xxx 外部依赖 / 5xxx 系统）。前端判别式收口在 `http.ts` 响应拦截器——抛 `ApiError`（携带 `code` 与可展示 `message`），调用方只管 catch。HTTP 非 200 表示"请求未达业务"（网络/路由故障），不走此语义。
_Avoid_: 用 HTTP 状态码判业务失败（后端一律 200）；用 `data` 是否存在判失败

### 车队统计

**车队周报**:
按自然周（周一 ~ 周日）聚合的车队战报：总览 + 七榜单 + 名场面 + AI 锐评，默认展示本周（ADR 0010）。
_Avoid_: 周战绩、weekly report

**周标签**:
一周的展示区间文案，格式 "2026-08-24 ~ 2026-08-30"（周一 ~ 周日，东八区）。

**周锚点**:
用于定位某一周的 ISO 日期（该周内任意一天均可，后端折算到周一）。

**名场面**:
从本周对局抽取的高光时刻：五杀时刻、最大翻盘、最长连败、单局最高击杀，四项均可能缺省。

**AI 锐评**:
后端 AI 生成的本周车队点评；AI 不可用时为 null，页面必须优雅降级隐藏。当前周支持强制刷新（ADR 0010）；历史周锐评已归档不可变，页面不提供刷新入口。

**分享图**:
周报页一键生成的 PNG 图片（canvas 绘制，发群用）；全部文案行由 `buildShareLines` 唯一产出。

**成员卡**:
单个成员的常驻数据面板：近 8 周成长曲线（周场次/胜率/场均 op_score）+ 英雄基线对比（本人 vs 全库同英雄）。

**op_score**:
对局评分引擎给出的单局评分，榜单与成员卡均以场均值呈现（保留两位小数）。
_Avoid_: 评分（与 MVP 评选分歧义时一律写 op_score）

### 图标资源

**图标本地化**:
把游戏图标（英雄头像、装备、召唤师技能、符文、海克斯强化）批量同步到自有服务器，
运行时本地优先、外部 CDN 兜底（ADR 0004）。召唤师头像因数量随版本增长、单页仅 1 枚而排除。
_Avoid_: 图标缓存（本词指服务器侧资源镜像，不是浏览器缓存）

**同步脚本**:
`npm run sync:icons`——从 CDragon/gtimg 批量拉取图标到 `public/icons/` 的手动维护脚本，
游戏版本更新后跑一次；图标目录不进 git，由 Docker 构建期执行脚本注入。

**降级链**:
CdnImage 的图标源候选序列，逐级回退直到占位符。如装备：本地 → DDragon → CDragon。
_Avoid_: 双源（旧词，仅指两级链；链长可变后统称降级链）

### 榜单维度（七榜）

口径与后端 TeamStatsService 一一对齐：

**出勤榜**:
按车队对局场次数降序，同场次按胜场降序。

**MVP 榜**:
按 MVP + SVP（后端称 ACE）次数降序，同次数按评选总分降序；只收录拿过称号的成员。

**op_score 榜**:
按场均 op_score 降序。

**战犯榜**:
按场均 op_score **升序**（越低越"战犯"），展示值即场均 op_score，附带最差一局作代表局。

**送头王**:
按场均阵亡数降序。

**Carry 王**:
按场均击杀参与率降序，附带伤害占比。

**绝活榜**:
成员 × 英雄（场次 ≥ 2）按场均 op_score 降序，页面按英雄分组展示；条目携带英雄 ID/名称/场次/胜场。
_Avoid_: 英雄榜

### 赛季资料（/season-archive）

**赛季资料**:
赛季维度的**容器页**，只有「版本趋势」一个 TAB（英雄 × 海克斯强化统计）。与 `/season-report`（赛季报告）是**两个独立投影，互不吸收**——前者主轴是英雄×强化，后者主轴是版本→胜率；两页经页头链接互跳（决策见后端 ADR 0014 关联评审）。
_Avoid_: 把赛季报告的区块搬进来；两页合并的中间态

**赛季**:
在本页面即**游戏版本**（如 `16.15`），页面上直接显示版本号；不存在独立的赛季目录或官方赛段边界。
版本筛选器的选项**动态取自库里有数据的版本**，不硬编码；页面默认选中**最新有数据的版本**，切换版本不重新发请求以外的前端状态重置。
_Avoid_: 把榜单中心写死的 `VERSION_OPTIONS` 那 7 个版本当全集

**出场率**:
某强化在某英雄对局中被持有的次数 ÷ 该英雄对局数。分子为每条 `playerAugment1-6` 的非零 ID，一局最多贡献 6 次，故同页出场率之和远大于 100%——页面必须带口径说明。
**不是选取率**：三选一被放弃的候选不留痕迹，候选集无法还原。
_Avoid_: 页面文案写"选取率"（会被按 100% 读）；隐藏总和大于 100% 这一事实

**强化小样本**:
局数 < 3 的格子降透明度并沉底（不宜用榜单搭档矩阵的 5 局门槛：本页样本更薄，5 会整页标灰而失去提示意义）。
默认按**出场次数降序**，表头可切换按胜率/出场率排序——按胜率默认会让"2 局 100%"顶在首屏。

**版本趋势 TAB（Trend Tab）**:
`/season-archive` 的核心视图：左列英雄列表（按纳入对局数降序），右侧当前所选版本的强化榜 + 行内悬停弹出的跨版本胜率走势卡。
表格展示**所选版本**的局数/胜率/出场率；历史版本数据只在悬停走势卡中出现（曲线断点 = 该版本无数据，当前版本以金色虚线标记）。
原型评审（season-archive-trend-prototype.html，三版 A/B/C）：选 A 改——走势以 hover 卡呈现而非常驻曲线区。
_Avoid_: 常驻的多强化曲线对比区（信息密度低且遮挡表格）；走势卡遮挡表格内容（跟随鼠标、出界翻转）

